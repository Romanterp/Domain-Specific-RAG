# Scraping
playwright>=1.40.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# PDF processing
pymupdf>=1.24.0       # fitz - fast PDF text extraction
pdfplumber>=0.11.0    # fallback PDF extraction

# NLP / text processing
langdetect>=1.0.9
scikit-learn>=1.4.0   # TF-IDF / BM25 keyword extraction
spacy>=3.7.0          # NER for entity extraction

# Data handling
pydantic>=2.5.0       # structured metadata schema
tqdm>=4.66.0          # progress bars

# LLM integration
openai>=1.0.0         # compatible with OLMo / local LLM APIs
